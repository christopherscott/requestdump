{
  "name": "application-name",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "export PORT=26675; forever start -al forever.log -o out.log -e err.log requestdump.js"
  },
  "dependencies": {
    "express": "3.0.0rc1",
    "jade": "*"
  }
}
